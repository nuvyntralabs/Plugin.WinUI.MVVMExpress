# WinUI MVVMExpress for Visual Studio

Tools → **WinUI MVVMExpress** → Create New App… / Add Page…

Installs `Plugin.WinUI.MVVMExpress.Templates` and runs `dotnet new`. Marketplace upload is manual from the `ide-extensions` workflow artifact.
